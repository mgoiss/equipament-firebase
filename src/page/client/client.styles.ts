import { Box } from "@mui/material";
import styled from "styled-components";
import Styled from "styled-components";

export const FormClient = styled(Box).attrs({
  "& .MuiTextField-root": { m: 1, width: "25ch" },
  component: "form",
  autoComplete: "off",
})``;
